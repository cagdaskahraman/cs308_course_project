# PostgreSQL — copy to `.env` and adjust for your environment
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=postgres

# JWT (signing must match JwtStrategy / AuthModule)
JWT_SECRET=change-me-to-a-long-random-string
